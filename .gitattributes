# Normalize line endings: store LF in the repo, check out native on each OS.
* text=auto eol=lf

# Shell scripts and service units must stay LF even on Windows checkouts.
*.sh text eol=lf
*.service text eol=lf

# Binary assets — never touch.
*.png binary
